<%= form_with(model: slide) do |form| %>
  <% if slide.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(slide.errors.count, "error") %> prohibited this slide from being saved:</h2>

      <ul>
        <% slide.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :titulo, style: "display: block" %>
    <%= form.text_field :titulo %>
  </div>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_area :descricao %>
  </div>

  <div>
    <%= form.label :slide, style: "display: block" %>
    <%= form.file_field :url_slide, multiple: false %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
