<p style="color: green"><%= notice %></p>

<h1>Cadastro de Imagens (SlideShow)</h1>

<div id="slides">
  <% @slides.each do |slide| %>
    <%= render slide %>
    <p>
      <%= link_to "Show this slide", slide %>
    </p>
  <% end %>
</div>

<%= link_to "Novo slide", new_slide_path %>
